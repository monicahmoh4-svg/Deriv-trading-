# Local dev defaults to the Vite proxy (see vite.config.js), so these can stay
# unset locally. Set both on Vercel to point at your deployed Render backend.

VITE_API_URL=https://your-backend.onrender.com/api
VITE_SOCKET_URL=https://your-backend.onrender.com
